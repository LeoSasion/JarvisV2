# Windows 10 native tests

Add tests here alongside the first Win10-specific implementation. Start with
an own-process visual probe and read-only host fixtures; no test may load a
module into Explorer without a separately approved live gate.
